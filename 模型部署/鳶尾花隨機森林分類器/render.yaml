services:
  - type: web
    name: iris-random-forest-classifier
    env: python
    buildCommand: pip install -r requirements.txt && python train_save.py
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
